import Referrals from "@/components/referrals/ReferralsComponent";

const page = () => {
  return (
    <main className="bg-primary min-h-screen">
      <Referrals />
    </main>
  );
};

export default page;
