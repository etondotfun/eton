import Profile from "@/components/profile/ProfileComponent";

const page = () => {
  return (
    <main className="bg-primary min-h-screen">
      <Profile />
    </main>
  );
};

export default page;
